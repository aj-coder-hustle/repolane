## What changed

<!-- One or two sentences. -->

## Why

<!-- The problem this solves. If there is an issue, link it. -->

## Checks

- [ ] `bash scripts/hooks/test-guard.sh` still passes (179/179) — required if `scripts/hooks/guard.py` changed
- [ ] A test case was added for the rule this changes — required if `scripts/hooks/guard.py` changed
- [ ] Docs updated to match the new behaviour (`docs/`, the folder READMEs, `CHANGELOG.md`)
- [ ] No new dependencies — bash and stdlib Python only
