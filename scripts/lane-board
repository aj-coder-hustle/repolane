#!/usr/bin/env bash
# lane-board — local web UI for lanes (127.0.0.1 only). Ctrl-C stops it.
exec python3 "$(python3 -c 'import os,sys;print(os.path.dirname(os.path.realpath(sys.argv[1])))' "$0")/board/server.py" "$@"
