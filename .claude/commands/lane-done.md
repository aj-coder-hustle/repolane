---
description: Finish a lane (refuses if dirty/unpushed)
allowed-tools: Bash(lane-done:*)
---
Output of `lane-done $ARGUMENTS`:
!`"$CLAUDE_PROJECT_DIR"/bin/lane-done $ARGUMENTS 2>&1`

Show the output above verbatim in a code block. Add nothing unless it contains an error or a line marked !!, in which case add one sentence on the fix.
