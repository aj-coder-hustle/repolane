---
description: Resume a lane: recreate worktrees and print state
allowed-tools: Bash(lane-resume:*)
---
Output of `lane-resume $ARGUMENTS`:
!`"$CLAUDE_PROJECT_DIR"/bin/lane-resume $ARGUMENTS 2>&1`

Show the output above verbatim in a code block. Add nothing unless it contains an error or a line marked !!, in which case add one sentence on the fix.
