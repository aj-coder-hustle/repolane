---
description: Board of repos and lanes (script output, verbatim)
allowed-tools: Bash(lane-status:*)
---
Output of `lane-status $ARGUMENTS`:
!`"$CLAUDE_PROJECT_DIR"/bin/lane-status $ARGUMENTS 2>&1`

Show the output above verbatim in a code block. Add nothing unless it contains an error or a line marked !!, in which case add one sentence on the fix.
