---
description: Brief on a lane: goal, note, commits, dirty, memory (script output)
allowed-tools: Bash(lane-brief:*)
---
Output of `lane-brief $ARGUMENTS`:
!`"$CLAUDE_PROJECT_DIR"/bin/lane-brief $ARGUMENTS 2>&1`

Show the output above verbatim in a code block. Add nothing unless it contains an error or a line marked !!, in which case add one sentence on the fix.
